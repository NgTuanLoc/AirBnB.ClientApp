import styled from 'styled-components';

const Home = () => {
	return (
		<Container className='section'>
			<h1>Home</h1>
		</Container>
	);
};

const Container = styled.main``;

export default Home;
